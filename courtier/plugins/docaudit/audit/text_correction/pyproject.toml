[project]
name = "courtier-plugin-text-correction"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["courtier"]

[tool.uv.sources]
courtier = { path = "../../../../../..", editable = true }
