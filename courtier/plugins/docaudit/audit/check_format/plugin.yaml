name: check_format
version: "1.0.0"
api: "2.0"
description: "Document format audit — validates formatting against GB/T 9704-2012 templates"
timeout_ms: 180000

# Tool declarations (incl. the model-visible input schema) and usage
# guidance are runtime-registered from entry.py / tools.py — no
# capabilities block here.
runtime:
  port: 9106
