[project]
name = "courtier-plugin-search"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "courtier-plugin-sdk",
    "elasticsearch>=8.17,<9",
    "httpx>=0.27",
]

[tool.uv.sources]
courtier-plugin-sdk = { path = "../../../libs/shared/plugin_sdk", editable = true }
