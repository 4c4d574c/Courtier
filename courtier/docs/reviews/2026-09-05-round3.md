# 代码审查报告 — 第 3 轮（2026-09-05 01:50）

R3 = 四个并行深审（本批修复自审 / 设置与配置体系 / 插件体系 / 数据库层）+ 修复。

## 1. 本批修复自审（bb271d4..HEAD，26 提交逐个 diff 复核）

结论：26 个提交中 20 个 ✓ 通过；其余各有小/中问题，全部当轮修复。抓出的最重要问题：

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| S-H1 | 高 | `92d5a07` 的 ES bulk 部分失败回滚只删 DB 行，已写入的部分 chunk 成为**永久不可删除的搜索孤儿**（delete_by_resource_id 依赖行）。回滚路径补 best-effort chunk 清理。 | 已修复（5e0d050） |
| S-M1 | 中 | `9bf860c` 的 wireEventSource CLOSED 恢复缺 generation 保护：loadSession 往返期间切走会话，迟到的 restoreSession 会覆盖新会话并关掉其流。 | 已修复（5e0d050） |
| S-M2 | 中 | `/api/stop` 的 `plugin_system.cancel_pending()` 在权限校验**之前**执行：任意登录用户可用任意 sessionId 取消全站插件在途请求（既有问题，非本批引入）。移到 get_owned 之后。 | 已修复（5e0d050） |
| S-M3 | 中低 | dompurify 声明范围仍是 ^3.4.11（含漏洞版 3.4.12），只靠 lockfile 挡住。floor 提至 ^3.4.14。 | 已修复（5e0d050） |
| S-L | 低 | 测试 setdefault 改 monkeypatch；useRunEvents 退避复位只认 run_status 消息的说明；docstring 过时等。 | 已修复/记录 |

## 2. 设置与配置体系

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| C-P1 | P1 | **密码重置不吊销 refresh token**：管理员重置/自助改密后，被窃取的 refresh cookie 最长 7 天内仍可无限换新 access token。新增 `revoke_all_refresh_tokens`，两个改密点接入。 | 已修复（5e0d050） |
| C-P2 | P1 | 生产首启向导的「内网门」未接入 trusted_proxies：反代后所有外网请求都表现为内网 IP。`_from_private_network` 改走 `rate_limiter.client_ip`。 | 已修复（5e0d050） |
| C-P3 | P2 | create_admin TOCTOU：并发首启请求可赛跑出第二个 admin。进程内锁串行化（单 worker 部署）。 | 已修复（05149db） |
| C-P4 | P2 | `trusted_proxies`/`tool_timeout_seconds` 落入不存在的 "advanced" 分组：可写不可见。归入 web/guards 组；顺带把 memory_* 归入模型组。 | 已修复（5e0d050） |
| C-P5 | P2 | 注销管线匿名化遗漏 `agent:<插件名>` actor（memory_changes 审计行未匿名化、title 留明文）。 | 记录，未修（需统一 actor 格式，涉数据迁移） |
| C-P6 | P2 | 注销不清 `.agent_logs` 审计文件（合规视角）。 | 记录，未修（保留策略需产品决策） |
| C-P7 | P3 | SettingsKeyMissing 裸 500；`_masked_settings_env` 篡改进程 environ；jwt_expire 死配置假热更；CORS env 逃逸静默忽略；MinIO 探测无超时；rebuild 组改索引名不重跑 init_index。 | 记录，未修 |

## 3. 插件体系

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| P-P1 | P1 | **未认证通知面**：任何 TCP 连接者可发 `plugin.shutdown`（掐断通道）、`plugin.host_services`（劫持全局 HostServiceClient，宿主后续会话内容流向攻击者 socket）、`request.cancel`。通知处理加鉴权门（仅 register 豁免）。 | 已修复（5e0d050） |
| P-P2 | P1 | detect_plagiarism/annotate 在事件循环内同步重计算（O(N²) 最长 170s+）→ 健康检查 90-105s 误杀连接、request.cancel 失效。两处改 `asyncio.to_thread`。 | 已修复（5e0d050） |
| P-P3 | P2 | media 的 ffprobe/ffmpeg 超时/取消后子进程不被 kill → 孤儿进程堆积。新增 `_communicate_and_reap`（kill + wait）。 | 已修复（5e0d050） |
| P-P4 | P2 | 宿主 JSONRPCClient 超时后的晚到响应永久滞留 `_response_buffer`（单行可达 64MiB，无上界）。 | 记录，未修 |
| P-P5 | P2 | SDK 无背压（每行无界起 task）；HostServiceClient 先写后注册 future 的丢响应竞态；sync handler 不可取消；host services 对 session_id 零校验。 | 记录，未修（协议级改动） |
| P-P6 | P3 | 规则引擎每 check 全量重编译正则；parse LRU 缓存键用临时路径永不命中且常驻 64 份文档；annotate 双路径临时文件泄漏；anydoc OCR 无页数上限且同步渲染；resolve_file 同名 TOCTOU/adhoc 目录堆积；proxies 对 base64 形路径误判与 minio:// 透传不校验会话。 | 记录，未修 |

## 4. 数据库层

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| D-P1 | P1 | 连接池零调优：无 pre_ping/recycle，夜间空闲后早高峰首批请求 "MySQL server has gone away"。 | 已修复（5e0d050，pre_ping+recycle 3600+池扩容） |
| D-P2 | P1 | 注销管线「最终事务」原子性被 `delete_user_memory` 内层 commit 破坏：记忆删了账号却可能还在。加 `commit: bool = True` 参数，管线传 False。 | 已修复（05149db） |
| D-P3 | P2 | refresh_tokens 无限增长（过期/撤销行永不清理）。登录事务顺带清扫过期 7 天以上的行。 | 已修复（5e0d050） |
| D-P4 | P2 | `delete_resource` 在打开的事务内做 ES 网络 I/O，占住连接池。 | 记录，未修（与 R1 的 fail-closed 顺序决策耦合，需对账任务设计） |
| D-P5 | P2 | `session_store.list_all` 全目录扫描 + 同步文件读阻塞事件循环。 | 记录，未修（建议内存索引/摘要文件，改动面较大） |
| D-P6 | P2 | legacy `documents.doc_id` 无唯一索引（并发重复行）；`bulk_create(returning=True)` MySQL 不支持（legacy 路径无生产调用方）。 | 记录，未修（legacy） |
| D-P7 | P3 | ensure_database_exists f-string 拼库名；resources 缺 (visibility, created_at) 索引；`_build_query` 静默丢弃未知过滤字段；naive/aware 混用约定靠手工维持；memory upsert 检查-后-插入竞态。 | 记录，未修 |

## 迁移链验证（无问题）

`alembic heads` 单头、链线性；关键表（users/refresh_tokens/resources/memory/deletion_*/settings/settings_changes/format_templates）迁移与模型逐一比对一致。

## R3 修复后基线

- pytest：**2343 passed**，0 failed
- ruff：0；webui lint+22 测试脚本+build 全过
