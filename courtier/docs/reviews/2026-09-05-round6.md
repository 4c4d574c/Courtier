# 代码审查报告 — 第 6 轮（2026-09-05 04:45）

R6 = 两路并行（R4/R5 修复独立验证 / 存储·ES·遥测·文档准确性）+ 修复。

## 1. R4/R5 修复独立验证

8 个核查点 7 ✓。最重要发现：
- **R6-HIGH（自引入）**：R5 的 Dockerfile CMD 改 main.py 后，镜像没拷贝 `alembic.ini`/`alembic/` → 容器 crash-loop（"healthy-but-empty" 变成永远起不来）。已补 COPY（39b3880）。这正是循环审查的价值：每轮修复本身也要被审。
- R6-MED：注入器剥离的 `all(...)` 条件只拦全部伪造，部分伪造（只传 `_caller_uid`）原样穿透 → 改为逐个剥离（39b3880）。
- 其余 ✓：set_rules per-owner（dict 保序、per-request 重建无 clone 问题）、upsert 回退在 REPEATABLE READ 下 rollback 后新快照可见、hold_dispatch 死锁核查（emit_terminal 无锁重入；stop 路径同构；queued-run 无此需要）、hint 替换的抑制窗口语义、Dockerfile main.py 的 sys.path 注入无害。

## 2. 新发现与处置

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| V-P1 | 高 | `Dockerfile:30-36` | 缺 alembic 文件（见上）。 | 已修复（39b3880） |
| V-P2 | 中 | `tools/registry.py:826` | host-injected 参数部分伪造穿透。 | 已修复（39b3880） |
| V-P3 | 中 | `telemetry/metrics.py:30` | `unit="tokens"` 使导出序列变成 `llm_token_usage_tokens_total`，与文档名不符。 | 已修复（39b3880） |
| V-P4 | 中 | `docs/operations/metrics-alert-design.md` | 全部指标名带错误 `courtier_` 前缀，照抄的告警永不触发；两处使用位置失实。 | 已修复（前缀，39b3880；使用位置记录） |
| V-P5 | 中 | `es/client.py:253` 等 | 删除/更新/append 只打写索引，搜索覆盖别名：reindex 换绑窗口期留脏数据/404。 | 记录，未修（改打别名是行为变更，需与 reindex 脚本联调） |
| V-P6 | 中 | `es/client.py:139` | `resolve_write_index` 忽略 `is_write_index` 标志，双索引窗口随机选。 | 记录，未修（同上） |
| V-P7 | 低 | `telemetry/tracer.py:213` | tool_span 记录伪造异常，真实栈丢失。 | 已修复（39b3880） |
| V-P8 | 低 | `storage/client.py:91` | `object_exists` 吞一切异常当"不存在"→ 预览路径盲目重跑转换。 | 记录，未修（改抛错会把存储故障变成 500，需配套降级设计） |
| V-P9 | 低 | `app.py:112/145` | FernetCodec 双构建双读 env。 | 已修复（构建一次复用） |
| V-P10 | 低 | `runtime/activation.py:232` | 重名技能静默跳过不打日志。 | 已修复（info 日志） |
| V-P11 | 低 | `telemetry/decorators.py` | 死代码装饰器且 kwargs.pop 抽参数、原始参数入 span。 | 记录，未修（直接删除即可，白天处理） |
| V-P12 | 流程 | tests | 本批 4 项修复缺回归测试。 | 已补 set_rules 多 owner 测试（95c9197 后续）；其余随白天补 |
| V-P13 | P3 | 文档 | e2e 文档 `?token=` 承载顺序过时。 | 已修复（39b3880） |

## 抽查通过（文档↔代码一致性）

guardrails.md 的 scope 派发/tool_call fail-closed/内置守卫播种、settings-and-secrets.md 的 CORS 救援与分组、memory-db-partition-plan.md 权限矩阵、setup 门 client_ip 接入、span 层级与 SSE 水印常量——与代码一致。

## 追加（终审 + 晚批修复，05:00-05:25）

终审（33 提交集成走查）结论：跨修复无冲突、锁序无死锁、报告与代码一致、树干净。

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| F-P1 | 中 | `sse_adapter.py:280` | pause 在 dispatch 锁内自旋 × hold_dispatch（6aa0f95）= 活锁：paused run 永远无法 complete/stop，直到 /api/resume。暂停等待移到取锁之前（监听器边界 + subagent 直接回调入口）。 | 已修复（e301f06，pause 测试重写为监听器语义） |
| F-P2 | 低-中 | `sse_adapter.py:458` | `subagent.event` 总线分支死代码（发布 key 是 `subagent_event`），且若被"修好"会在非重入锁上自死锁。删除。 | 已修复（e301f06） |
| F-P3 | 低 | round6 报告 V-P11 | decorators.py 实际已删（8b45c43），状态已更新。 | 已修复 |
| F-P4 | 低 | `file_service.py:408` 等 | 转码后 size_bytes 仍按原始字节注册；转码产物依赖 24h 生命周期兜底；ES 回滚不清 MinIO 原件、删资源不清预览缓存（md5 键孤儿）。 | 记录，未修 |
| F-P5 | 低 | `middleware/auth.py:77` | `?token=` 门控含整个 `/api/sessions/` 前缀（非 EventSource 子路由也接受）。 | 记录，未修（可收窄到 */events） |
| F-P6 | 低 | `run_manager.py:487` | queued run 的 status 落库与 stop 并发时后写者胜（内存态正确，自愈）。 | 记录，未修 |

## 追加修复（R7 验证轮，05:15-06:14）

R7 验证抓到 **2 个 HIGH**（都在 39b3880 自己引入的部署修复里）并全部修复：

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| R7-H1 | 高 | Dockerfile 没拷贝 `main.py`——CMD 指向的入口文件不在镜像里。 | 已修复（dfd44aa） |
| R7-H2 | 高 | alembic 只在 dev 依赖组，`uv sync --no-dev` 不装 → main.py 的 import 在容器内必炸。移入运行时依赖。 | 已修复（dfd44aa） |
| R7-H3 | 高 | `app.py` 的 `from ..settings_store import` 指向不存在的 `courtier.agent.settings_store`——staging/production lifespan 启动必崩（dev 分支不执行该 import，测试全绿也没暴露——正是本轮补的 lifespan 冒烟测试存在的意义）。 | 已修复（8dfcec4，绝对导入 + lifespan 守卫冒烟测试 ×3） |
| R7-M1 | 中 | dfd44aa 声称恢复 dev 模式设置加密但 diff 未含 app.py——codec 现在无条件解析，dev 有 key 即加密、无 key 回退 env（旧行为），staging/production 缺 key fail-fast。 | 已修复（8dfcec4） |
| R7-M2 | 低-中 | removeAttachment 的 `includes(removed.name)` 恒假（含文件名的错误对应从未入列的文件），移除附件永远不清模态错误 → 改为按错误前缀精确清除。 | 已修复（8dfcec4） |
| R7-L | 低 | 迁移 `c4d9e0f1a2b3`：refresh_tokens.expires_at 与 resources (visibility, created_at) 索引（登录清扫全表扫描、资源列表过滤+排序）。 | 已修复（9426d34） |

## 最终基线（06:14）

- pytest **2342 passed** / 0 failed（比 06:00 基线 2339 多 3 个 lifespan 守卫测试）
- ruff 0；mypy 60→55（剩余为类型严格度噪音）
- webui lint + 22 测试脚本 + build 全过；npm audit 0
- `alembic heads` 单头链完整
- git 工作树干净，33+ 提交全部入库

## 交接清单（白天优先级）

1. EventBus 有损队列可能丢关键事件（按事件类别分级队列）
2. control stop 的 cancel_pending 插件级全局（request→session 跟踪）
3. SSE 任务全文走 URL（POST SSE 迁移）
4. get_artifact 编号 ref 重解析后的替换语义（注册层保留编号条目）
5. 流式 tool_calls 回退幽灵文本（新事件语义）
6. 注销管线 agent:<插件> actor 匿名化 + .agent_logs 清理
7. Dockerfile.plugins 非 root 用户（需可构建环境验证）+ uv 镜像 digest
8. API 层 200+ 中文硬编码 → ui.* 键空间（立项）
9. artifacts docaudit 解耦、auth 服务层抽取（架构项）


---

# 追加：第 7 轮（设置视图审查 + 验证，05:30-06:45）

## 审查（SystemSettings/Setup/Register/Login/AdminLayout/审批·插件·技能·个人页）

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| S-P1 | P1 | `SystemSettings.vue:1789` + `client.ts:122` | 保存设置**完全静默失败**：save() 无 catch（422 校验/连接探测失败 → unhandled rejection），且 parseErrorDetail 不识别 `{message, errors[]}` 形态只剩状态码。 | 已修复（a913295） |
| S-P2 | P2 | `utils/settingsForm.ts:86` | 「清除并回退」标志吞掉之后输入的新值（发送 null 而非新值），管理员以为已保存。改为：字段仍为空才发 null，有输入按编辑处理。 | 已修复（a913295，测试更新为新契约 + 补空值分支用例） |
| S-P3 | P2 | `ApprovalManagement.vue` | 批准注销（不可逆销毁全部数据）无二次确认。加确认对话框。 | 已修复（a913295） |
| S-P4 | P2 | `PluginMarketView.vue:196` | DISCONNECTED 被当过渡态：崩溃插件永久禁用按钮、无恢复路径。移出 transitional + 徽标改 error。 | 已修复（a913295） |
| S-P5 | P3 | `client.ts:80` | 连接测试/设置保存用默认 10s 超时，慢 LLM/ES 假失败。改 60s。 | 已修复（a913295） |
| S-P6 | P3 | `ApprovalManagement.vue:197` | 注册列表加载失败连带跳过注销申请加载（两个独立接口）。 | 已修复（a913295） |
| S-P7 | P3 | `SetupView.vue` | 完成后表单仍可提交。加 v-if 收起。 | 已修复（a913295） |
| S-P8 | P3 | `SystemSettings.vue:1535` | 池端点测试结果不随后续编辑失效（旧「连通」标签误导）。 | 记录，未修 |
| S-P9 | P3 | `SystemSettings.vue:1728` | 「放弃更改」重置全部分类的未保存编辑。 | 记录，未修 |

## 追加：R8 验证（07:00）

| # | 严重度 | 问题 | 状态 |
|---|--------|------|------|
| R8-M | 中 | parseErrorDetail 的对象分支假定 errors 是数组；探活失败 422 的 errors 是 `{es: "...", minio: "..."}` 映射 → 解析器抛 TypeError 掩盖真实失败。改双形态兼容。 | 已修复 |
| R8-L | 低 | bool 设置字段「清除后拨动」仍发 null（cleared 标志不随编辑复位）。模板 @change 复位 cleared。 | 已修复 |
| R8-I | info | 死代码 guard 行、注释缩进、memory upsert 竞态败者多一条审计行。前两项已清；审计噪声记录。 | 已清理/记录 |

## 基线（R8 后，07:05）

pytest 2342 passed / 0 failed；ruff 0；webui lint + 22 测试 + build 全过；npm audit 0；alembic 单头。


---

# 终态（2026-09-05 07:15，循环按计划停止前最后核验）

- 后端：`uv run pytest -m "not integration"` → **2342 passed / 0 failed**（马拉松起点 2302）
- lint：ruff **0**（起点 94）；mypy 60→55（余为类型严格度噪音）
- 前端：eslint + 22 断言脚本 + vite build 全过；npm audit 0 漏洞（起点 5 个 high + dompurify XSS）
- 迁移链：alembic 单头完整（新增 c4d9e0f1a2b3 操作索引）
- 依赖：Dockerfile 入口完整（main.py + alembic 运行时依赖 + 全部文件拷贝）
- 集成终审（33+ 提交走查）：跨修复无冲突、锁序无死锁、「可交班」
- integration 标记测试需真实 ES/MinIO 凭据（401/403），本环境无信号，白天线跑
