# 代码审查报告 — 第 2 轮（2026-09-05 00:45）

R2 = 三个并行深审（核心运行时 / 前端深层 / R1 修复回归验证）+ 修复。

## R1 修复回归验证结论

8 项 R1 修复**全部验证通过**（setup key 权威化、locked_until 时区、SSE 隔离、转码白名单、有界读取、ES 校验、插件超时/transfer bucket、refresh 家族撤销持久化）。验证中另发现 4 个 P3（已修：见下）。

## 新发现与处置

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| R2-P1 | P1 | `agent/runtime/runtime.py:397` | delegate 吞 CancelledError：/api/stop 无法停止子代理，run 错写 completed 且继续烧 token。按 `Task.cancelling()` 区分外层取消（重抛）与 `terminate()` 显式取消（保留报错继续语义）。 | 已修复（28c394b） |
| R2-P2 | P2 | `agent/core/context_manager.py:574` | 压缩 fallback 丢系统提示词（且续轮重建不生效）。 | 已修复（28c394b，测试更新） |
| R2-P3 | P2 | `services/run_manager.py` | `_runs` 只按会话懒回收：无人回访的会话永久钉住完整事件日志（≤8MB/个）→ 内存无界增长。 | 已修复（reaper，28c394b + 4d240d2 后续） |
| R2-P4 | P2 | `webui/composables/useAgentSession.ts:190` | wireEventSource onerror 缺 CLOSED 分支：初始连接失败（cookie 过期/429/后端重启）后 UI 永久卡「运行中」。 | 已修复（9bf860c） |
| R2-P5 | P2 | `webui/api/client.ts:246` | `api.refreshToken()` 绕过 single-flight：并发刷新会二次提交已轮换的 refresh cookie → 触发后端重用检测（R1 修复后该检测已真正生效！）吊销全家。 | 已修复（9bf860c） |
| R2-P6 | P2 | 前后端 `editAndResend` | 思考记录截断 off-by-one（turnIndex 1-based，用了 `<`），前后端同病，测试 fixture 用 0-based 数据掩盖了缺陷。 | 已修复（9bf860c） |
| R2-P7 | P2 | 前端 | 跨会话状态残留（refusalNotice/pendingConfirmations/queuePosition/guardEvents/hintEvents）→ A 会话横幅/确认卡出现在 B。 | 已修复（9bf860c） |
| R2-P8 | P2 | `views/HomeView.vue` | useRunEvents 订阅解绑函数被丢弃：每次进 Home 叠一层监听（N 次访问 = N 份 toast）。 | 已修复（9bf860c） |
| R2-P9 | P2 | 前端 `stop()` | 无 generation 保护：await 期间切换会话会把新会话状态改掉/关掉新流；且失败仍置 completed。 | 已修复（9bf860c） |
| R2-P10 | P3 | `routes/setup.py:66` | hmac.compare_digest 对非 ASCII str 抛 TypeError → 500。 | 已修复（比较 bytes，28c394b） |
| R2-P11 | P3 | `services/file_service.py:299` | 转码取回失败把原始异常透给客户端。 | 已修复（28c394b） |
| R2-P12 | P3 | `plugin/manager.py:77` | presign 有效期 7 天 vs transfer bucket 24h 生命周期。 | 已修复（≤23h，28c394b） |
| R2-P13 | P3 | `core/context_manager.py:208` | fork() 丢媒体 token 估算参数。 | 已修复（28c394b） |
| R2-P14 | P3 | `core/loop.py:494` | forced-first-tool-call 空响应时静默跳过强制解析。 | 已修复（28c394b） |
| R2-P15 | P2 | `run_manager.py:742` + `sse_adapter.py:1078` | 终端事件 reserve 后经多次 await 才 append，在线读者可能因 seq 倒挂丢弃终态事件。 | 记录，未修（需 emit 串行化或 reader 侧重排，改动面大） |
| R2-P16 | P2 | `core/model.py:412` | 流式 finish_reason=tool_calls 回退：前端留幽灵文本、真实结论不流式。 | 记录，未修（需新增事件语义） |
| R2-P17 | P3 | `runtime/runtime.py:226` | `_handles`/`_cumulative_runtime` 只增不清（当前 per-request 生命周期无实害，session 级复用前须修）。 | 记录 |
| R2-P18 | P3 | `core/loop.py:1186` | turn_pending_record 与注释不变量矛盾（blocked 轮也入树）。 | 记录（行为无害） |
| R2-P19 | P3 | `core/loop.py:1069` | citation 事件偏移与模型侧标注偏移两路独立 load 可能失步。 | 记录 |
| R2-P20 | P3 | 前端其余 | SSE 任务全文走 URL（设计取舍）、resolveConfirmation 回滚边界、坏 JSON 污染整轮、useRunEvents 无退避、上传失败丢输入、purifier 非 DOM 直通注释。 | 记录，部分转入 R3 |

## 未修说明

- R2-P15/P16 需要跨模块事件协议改动，夜间批量修改风险大于收益，留给白天立项。
- 确认「无自动超时」是既有设计决策（见 docs/architecture confirmation 计划），不作为缺陷处理。

## 基线对比（R2 修复后）

- pytest：**2343 passed**，0 failed
- webui：lint + 22 个测试脚本 + build 全过
- ruff：0
