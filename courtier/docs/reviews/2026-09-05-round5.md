# 代码审查报告 — 第 5 轮（2026-09-05 04:05）

R5 = 两路并行（记忆子系统与守护管线 / 部署面与入口）+ 修复。

## 1. 记忆子系统与守护管线

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| M-P1 | P1 | `core/memory_manager.py:224` | recall 注入预算是整段丢弃语义：用户层段超 1500 字符（重度用户 4-5 条记忆即触发）→ 本轮连全局层都不注入，功能对目标用户静默失效。 | 已修复（逐条目贪心填充，5e93238） |
| M-P2 | P2 | `services/memory_service.py:257` | upsert find-then-write 撞 `uq_memory_address` 未处理 → 并发双写 500/工具报错。捕获 IntegrityError 回退合并。 | 已修复（5e93238） |
| M-P3 | P2 | `config.py:591` | `guardrail_guards` 每次快照组合都重新 import 校验：守卫类重构后陈旧 class_path 让启动与设置 API 整体失败，与运行时「坏声明跳过+告警」的容错语义矛盾。 | 记录，未修（需区分保存/加载路径的校验强度，涉 Settings 校验器语义） |
| M-P4 | P3 | `plugin/manager.py:687` | memory 权限矩阵 OR 判定：只声明 `read:memory` 的插件可改写/删除记忆。改按 action 分流（write/delete 需 write:memory）。 | 已修复（5e93238） |
| M-P5 | P3 | `tools/registry.py:816` | 注入器表为空时提前返回、不剥离模型伪造的 x-host-injected 参数（7647282 同类形态）。空表也剥离。 | 已修复（5e93238） |
| M-P6 | P3 | `core/memory_manager.py:176` | 旧轮 recall hint 滞留历史逐轮累积（20 轮 ≈3 万字符冗余）。 | 记录，未修（需「替换上一条 hint」语义） |
| M-P7 | P3 | `services/memory_service.py:226` | 分页排序无稳定 tiebreaker。加 `id.desc()`。 | 已修复（5e93238） |
| M-P8 | P3 | `services/memory_service.py:375` | clear_layer 审计集合与删除集合之间存在并发窗口。 | 记录，未修 |
| M-P9 | P3 | `agent_service.py:486` | path_policy 自声明只在 build_agent 扫一次，晚注册工具不受管（当前无声明方，潜伏）。 | 记录，未修 |
| M-P10 | P3 | `permission_guards.py` vs `file_tools.py:189` | WriteTool 检查后打开无 O_NOFOLLOW（TOCTOU，前提是工作区内有可竞争写者）。 | 记录，未修 |

**核实为完整**：x-host-injected 身份通道（schema 剥离 + 派发剥离回填 + 7647282 修复）、层级权限（用户层互不可见/全局层 admin-only）、ConfirmationGuard fail-closed、坏 guard 声明运行时跳过。

## 2. 部署面与入口

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| D-P1 | P1 | `Dockerfile:58` | CMD 直接起 uvicorn 绕过 main.py 的迁移运行器：容器化新部署「健康但空 schema」，升级期 schema 漂移。CMD 改走 main.py。 | 已修复（5e93238） |
| D-P2 | P1 | `app.py:96` + `settings_store.py:519` | 生产缺 MYSQL_URL/缺 COURTIER_SETTINGS_KEY 时「半死」启动：健康检查 ok 但登录永久 500（JWT 引导静默跳过）。staging/production fail-fast。 | 已修复（5e93238） |
| D-P3 | P2 | `docker-compose.yml:32` | MySQL 健康检查走 localhost（初始化期临时 socket 服务器假阳性）；root 密码内嵌命令。改 127.0.0.1 强制 TCP（密码内嵌另见记录）。 | 已修复（5e93238） |
| D-P4 | P2 | `scripts/minio_plugin_io.py:53` | .env 加载只取 MINIO_ 前缀，必需的 COURTIER_PLUGIN_MINIO_* 不在内，按文档运行必失败。 | 已修复（5e93238 + .env.example 补键） |
| D-P5 | P2 | `app.py:437` | 静态挂载依赖 CWD（`Path(".")`），非仓库根启动静默不挂载。改 `__file__` 推导默认值 + 未命中日志。 | 已修复（5e93238） |
| D-P6 | P2 | `Dockerfile.plugins` | 插件容器以 root 运行（处理不可信文档）。 | 记录，未修（镜像无法本地构建验证，需白天在可构建环境加 USER） |
| D-P7 | P2 | `app.py:331` + compose | /metrics 默认无鉴权 + 8000 端口发布到 0.0.0.0（其余均绑回环）。 | 记录，未修（R1 已记录「避免破坏现有 Grafana 抓取」；compose app 服务默认注释，绑回环改动低险，白天确认） |
| D-P8 | P3 | compose/脚本 | uv 仅 tag 无 digest；compose 无资源限制；minio 无 healthcheck、langfuse 未声明依赖；COURTIER_PLUGIN_TOKEN 无 :? 强制；.dockerignore 缺 .agent_cache；Dockerfile.env dev-only 未标注；dev-plugins 对早退子进程无感知。 | 记录，未修 |

## 核查为稳健

lifespan 顺序（DB→settings→plugins→清扫→ES init）与 docstring 一致；中间件栈序正确；全部常驻服务 restart 策略；端口除 8000 外均绑回环；signals 交由 uvicorn。

## 追加修复（R5 晚批，04:30-05:00）

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| M-P11 | P2 | `run_manager.py` 终态 ×4 + `sse_adapter.py` | 终态事件 seq 倒挂（= R2-P15）：reserve→persist→append 与 bus 监听器竞态，在线读者静默丢终态帧。`hold_dispatch()` 把 reserve→persist→emit 包进监听器分发锁；`drain_pending` 等待在途事件。 | 已修复（6aa0f95） |
| M-P12 | P3 | `core/memory_manager.py:176` | 旧轮 recall hint 逐轮累积（~3 万字符冗余）。新 hint 替换所有旧 hint。 | 已修复（cb2227d） |
| M-P13 | P2 | `tools/builtin/get_artifact.py:782` | $ref 编号 miss 时前缀兜底可能在重解析后静默替换为新文档数据。**尝试修复后回滚**：测试揭示这是持久层（编号 ref）与注册层（:latest）双层设计的必要回退，修复需让注册层保留编号条目或校验数据同源性——设计级，记录待立项。 | 记录，未修（回滚） |

## 基线（R5 后）

pytest 2343 passed / 0 failed；ruff 0。
