# 代码审查报告 — 第 4 轮（2026-09-05 03:00）

R4 = 三路并行（R3 修复独立验证 / 前端剩余区域 / 工件系统+域激活）+ 修复。

## 1. R3 修复独立验证（5e0d050 / 05149db / 58aab50）

11 个核查点全部 ✓，包括最担心的两项：
- `ensure_database_exists` 的库名校验被 `drivername != mysql` 闸门完全隔离，sqlite 文件路径不受影响；
- `revoke_all_refresh_tokens` 的 user_id 语义（admin 路径=被操作用户）与事务边界均正确。

验证中发现并当轮修复：
- `_delete_user_memory` 吞异常 + commit=False 组合下根因被 PendingRollbackError 掩盖 → 报告记录（原子性本身保住）；
- `memory_service.py` 双 docstring 死代码、`resource_service.py` 注释失准 → 清理。

## 2. 新发现与处置

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| R4-P1 | P1 | `runtime/activation.py:253` + `prompts/pipeline.py:156` | workflow_rules 整块覆盖：多域激活互相清掉对方规则（与"additive"承诺相反）；域无该模板时注入英文 FALLBACK 样板规则。 | 已修复（95c9197，按 owner 累积 + 无模板静默） |
| R4-P2 | P1 | `runtime/activation.py:227` | activate_domain 半途失败无回滚且重试必然再失败（同名技能 ValueError）→ 该域本会话永久无法激活。注册幂等化（跳过已注册）。 | 已修复（95c9197） |
| R4-P3 | P1 | `webui/utils/settingsForm.ts:103` | 设置表单清空 int/float 输入框静默提交 0（`Number("")===0`）。 | 已修复（95c9197） |
| R4-P4 | P1（前端） | `useAgentSession.ts` wireEventSource | 上一轮的 CLOSED 分支缺 generation 保护（自查补上，5e0d050 批次）。 | 已修复 |
| R4-P5 | P2 | 前端 `stop()` | 本地收尾缺失：stopped SSE 帧与 POST 竞态，disconnect 吞帧后工具卡永久「执行中」。补 `finalizeRunningOperations`（幂等）。 | 已修复（95c9197） |
| R4-P6 | P2 | `skills/registry.py:60` | scan 读文件无容错：与 admin 原地重写并发时整个 agent 构建失败（500）。捕获 OSError/UnicodeDecodeError 跳过。 | 已修复（a746f4e） |
| R4-P7 | P2 | 前端杂项 | AdminLayout 非管理员可见 admin-only「Agent能力」组；用户管理改角色失败下拉框不回滚（改刷新列表）；资源库列表加载失败静默显示「暂无」；maxSnapshotToolId 不扫嵌套子代理树（重放 id 碰撞）；connect() 不清 refusalNotice；确认回滚覆盖竞态新卡；initAuth 网络抖动把标签页永久登出（single-flight 网络异常改为上抛以区分「确定未登录」）。 | 全部已修复（95c9197） |
| R4-P8 | P2 | `core/event_bus.py:110` | 有损队列（drop_oldest）被当作持久化通道：token 流打满 1000 深队列时丢的可能是 tool.result/think.tool_calls 等关键事件（SSE+持久化双丢失）。 | 记录，未修（需按事件类别分级队列，协议级） |
| R4-P9 | P2 | `tools/builtin/get_artifact.py:782` | `$ref` 前缀兜底把另一工件数据当请求 ref 返回（二次解析后静默审错文档）。 | 记录，未修 |
| R4-P10 | 中 | `routes/control.py:73` | stop 的 cancel_pending 仍是插件级全局：拥有运行中会话的用户可取消其他用户在飞的插件请求。按会话取消需要 request→session 跟踪贯穿 proxies/client。 | 记录，未修（功能级） |
| R4-P11 | P2 | `courtier/plugin/client.py:184` | 超时后晚到响应滞留 `_response_buffer` 无上界。 | 已修复（58aab50，每 id 上限 4） |
| R4-P12 | P3 | 杂项 | MinIO 探测 5s 超时、CREATE DATABASE 库名白名单、`_build_query` 未知字段 raise（静默放行是越权形态）、session list_all 解析移出事件循环。 | 已修复（58aab50） |
| R4-P13 | P3 | 工件系统 | `register_artifact_schema` 全局可变单例无作用域；ScopedArtifactStore 死代码且权限弱于生产版；`register_cached_ref` fire-and-forget task 无强引用；persist 成功路径同步重写 hash-index；PromptEngine autoescape 转义模型可见文本；orchestrator.audit_results 覆盖式无人消费。 | 记录，未修 |

## 核查为稳健（R4 新覆盖区域）

工件投影幂等（content_hash 键）、会话隔离双保险（磁盘目录 + ES 文档 id 前缀）、EventBus per-run 实例与退订、域激活的 x-host-injected 剥离边界、confirmations 单线程无竞态、前端 throttle/date/numerals/守卫重定向、复用检测 commit-before-401 的回归测试真实性。

## 基线（R4 后）

pytest 2343 passed / 0 failed；ruff 0；webui lint+22 测试+build 全过。
