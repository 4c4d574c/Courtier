# 代码审查报告 — 第 1 轮（2026-09-04 23:00）

审查方式：五个并行维度深审（安全 / 错误处理 / 架构 / 依赖 / 测试覆盖），全部发现经人工回源核实后才列入。
基线状态：pytest 2302 passed / 6 skipped（非 integration）；webui npm test 通过；ruff 94 个问题；mypy 60 个错误。
处置约定：每轮「审查 → 报告 → 修复 → 复审」循环；本轮报告中的「状态」列随修复推进更新。

严重度定义：P0 = 可直接利用/数据损坏；P1 = 高危，尽快修；P2 = 中危，计划内修；P3 = 低危/改进项。

---

## P1（高危）

| # | 维度 | 位置 | 问题 | 状态 |
|---|------|------|------|------|
| P1-1 | 安全 | `courtier/agent/api/routes/setup.py:57-69` | 生产首启向导的「内网来源」门在反代拓扑下失效：uvicorn 默认不解析 X-Forwarded-For，`request.client.host` 恒为反代的私网 IP，互联网请求被误判为内网，无需 `COURTIER_SETUP_KEY` 即可抢注首个 admin。 | 已修复 |
| P1-2 | 错误处理 | `courtier/agent/api/sse_adapter.py:250-259` | Event Bus 监听器无逐事件隔离：任一事件 dispatch 抛异常（如插件 metadata 不可 JSON 序列化）整个监听任务死亡且不重建，该 run 剩余事件既不进 SSE 也不持久化，前端表现为流静默中断、无终态事件。 | 已修复 |
| P1-3 | 依赖 | `webui`（DOMPurify 3.4.11） | DOMPurify ≤3.4.12 存在两个已知 XSS 绕过（CUSTOM_ELEMENT_HANDLING / IN_PLACE 钩子），它是 LLM 输出 markdown 的唯一 sanitizer。 | 已修复 |
| P1-4 | 依赖 | `docker-compose.yml:67` | `minio/minio:latest` 浮动 tag，存储全量文档数据，版本不可控。 | 已修复 |

## P2（中危）

| # | 维度 | 位置 | 问题 | 状态 |
|---|------|------|------|------|
| P2-1 | 错误处理 | `routes/auth.py:205-208` | 账号触发锁定后该用户所有登录请求 500：`locked_until`（DB 读回 naive）与 aware `now` 比较抛 TypeError。 | 已修复 |
| P2-2 | 安全 | `services/file_service.py:244-257` | 视频转码回读只检查 `minio://` 前缀即用宿主全权凭据 `get_object`，被攻陷插件可指定任意 bucket 投递任意对象给用户。缺 transfer-bucket allowlist（对比 `plugin/manager.py:617-623` presign 有）。 | 已修复 |
| P2-3 | 错误处理 | `es/client.py:196-202` | `bulk` 返回值被丢弃：item 级失败（HTTP 200 + `errors:true`）被无视，资源标记 ready 但 0 条切片入库；`delete_by_query`（:226-242）的 `failures` 同样不检查，「已删除」资源仍可被搜索。 | 已修复 |
| P2-4 | 安全 | `services/file_service.py:283-291`、`resource_service.py:232` | 上传先整读进内存再校验 50MB 上限，且无请求体上限 → 内存耗尽 DoS。 | 已修复 |
| P2-5 | 安全 | `api/rate_limiter.py:14-29` | 未认证路由按 `request.client.host` 限速，反代下全局共享一个桶 → 登录/注册可被单攻击者持久锁死。 | 已修复（可信代理 XFF） |
| P2-6 | 错误处理 | `plugin/manager.py:321-323` | 插件重连 `asyncio.open_connection` 无 timeout，防火墙 DROP 时单次连接挂起约 2 分钟。 | 已修复 |
| P2-7 | 错误处理 | `services/run_manager.py:497-503` | `stop()` 与 `_try_admit_waiting` 竞态：队列项被并发移出后 `remove(run)` 抛 ValueError → `/api/stop` 500。 | 已修复 |
| P2-8 | 错误处理 | `routes/sessions.py:198` | 续轮判断 `existing.turn_messages[-1]["text"]` 对空/畸形数据抛 IndexError/KeyError（高频路径 500）。 | 已修复 |
| P2-9 | 测试 | auth / control / profile / memory 路由 | refresh token 轮换（重用检测）、`/api/control/stop`、修改密码、`/api/memory` 全部 14 个端点零测试覆盖。 | 已修复（补测试） |
| P2-10 | 依赖 | `webui` 构建链 | vite ≤6.4.2 / postcss ≤8.5.22 等 4 个 high（开发期暴露面）。 | 已修复（npm audit fix） |
| P2-11 | 依赖 | `plugins/shared/search/pyproject.toml:8` | `elasticsearch>=8.0.0` 无上限，未来可解析到与 8.x 服务器不兼容的 9.x 客户端。 | 已修复 |
| P2-12 | 架构 | `courtier/db/load_doc.py:22`、`save_doc.py:9` | 从 `courtier.db` 包 `__init__` 反向导入，依赖 `__init__` 语句顺序才不炸的真实循环导入。 | 已修复 |

## P3（低危 / 改进项）

| # | 维度 | 位置 | 问题 | 状态 |
|---|------|------|------|------|
| P3-1 | 错误处理 | `plugin/manager.py:222,600,635,666,706` | 宿主→插件错误响应携带原始 `str(exc)`（可能含 SQL/路径），插件是半信任边界。 | 已修复 |
| P3-2 | 错误处理 | `tools/registry.py:405-407` | 工具 on_progress 异步回调异常被完全吞掉（连日志都没有）。 | 已修复 |
| P3-3 | 架构 | `courtier/common/`、`courtier/benchmarks/` | 零引用死代码包。 | 已修复（删除） |
| P3-4 | 文档 | 根 `AGENTS.md:39,491` | 引用不存在的 CLAUDE.md（两处），§6 目录树与实际不符。 | 已修复 |
| P3-5 | lint | 全仓 | ruff 94 个问题（E501×57、I001×19、F401×11、E402×4、F841×2、W293×1）。 | 已修复 |
| P3-6 | 类型 | 26 文件 | mypy 60 个错误（含真注释噪声音与少数需修的 union-attr/arg-type）。 | 部分修复 |
| P3-7 | 安全 | `middleware/auth.py:92-119` | 全部 `/api/*` 仍接受 `?token=` 查询参数传 JWT（token 进日志/历史）。 | 已修复（仅 SSE/metrics 保留） |
| P3-8 | 安全 | `libs/shared/plugin_sdk/files.py:150-171` | SDK `resolve_file` 不校验 bucket 是否为 transfer bucket，隔离完全依赖 IAM。 | 已修复（SDK 侧校验） |
| P3-9 | 错误处理 | `services/resource_service.py:321` | ES 错误原文透给前端（含主机名/索引名）。 | 已修复 |
| P3-10 | 依赖 | `Dockerfile`/`Dockerfile.plugins`/`Dockerfile.env`/compose 多处 | 镜像浮动 tag（`uv:latest` 被 COPY 进生产镜像、langfuse:3、clickhouse:24 等），未固定 digest。 | 部分修复（钉版本 tag） |
| P3-11 | 安全 | `resource_service.py:226-234` | 资源库上传无 magic-byte 校验（聊天通道有），恶意文件直达 PyMuPDF/LibreOffice。 | 已修复（复用 magic-byte 校验） |
| P3-12 | 架构 | `routes/auth.py`、`admin_users.py`、`profile.py`、`setup.py`、`account_deletion.py` | auth/用户管理安全关键链路无服务层，业务规则写在路由里。 | 记录，暂不修（大重构） |
| P3-13 | 架构 | `agent/artifacts/models.py:161-300` | 核心 artifacts 硬编码 8 个 `docaudit.*` schema，新领域必须改核心。 | 记录，暂不修（大重构） |
| P3-14 | 架构 | API 层 200+ 处 | 用户可见中文文案硬编码（HTTP/SSE/工具描述），绕过 PromptEngine/errors.*，「零硬编码 NL」不变量系统性违约。 | 记录，暂不修（需立项 ui.* 键空间） |
| P3-15 | 错误处理 | `guardrails/guardrail_system.py:243-249` | scope 级 guard 抛异常被跳过（fail-open），仅 per-call fail-closed；注释表明是刻意取舍。 | 记录，暂不修（设计决策） |
| P3-16 | 安全 | `app.py:330-342` | `/metrics` 未设 token 时匿名可读（依赖网络层隔离）。 | 记录，暂不修（避免破坏现有 Grafana 抓取） |
| P3-17 | 错误处理 | `storage/client.py:36-41` | MinIO 客户端未显式超时；`object_exists` 吞所有异常为 False。 | 记录，暂不修 |
| P3-18 | 测试 | `tests/agent/test_regression.py` | 回归套件被标 integration，默认永不执行。 | 已修复（去标） |
| P3-19 | 前端 | `useAgentSession.ts:510`、`client.ts:381`、`useRunEvents.ts:67` | stop 失败静默置 completed；loadSession 把 500 当 404；全局事件重连无退避。 | 记录，暂不修 |
| P3-20 | 依赖 | `pyproject.toml` | `aiomysql` 疑似冗余（实际驱动 asyncmy）；`openpyxl` 仅 scripts 用应移 dev 组。 | 记录，暂不修（避免动锁文件风险） |

## 核实为稳健、无需修复的重点区

JWT（HS256 白名单/exp 强校验/refresh 轮换+重用检测）、路由级鉴权全覆盖、admin 越权矩阵、SQL 全 ORM、`yaml.safe_load`、Jinja2 autoescape 且无 SSTI、subprocess 全数组无 shell、上传文件名服务端重生成、PathPolicyGuard resolve()+fail-closed、插件双向 token `hmac.compare_digest`、DOMPurify + CSP + nosniff、设置秘钥 Fernet + 只回 tail4、run_manager trace_id 模式、SSE 水印重放协议、外部调用超时覆盖（LLM/RPC/ES/embeddings/ffmpeg）。

---

## 修复日志（第 1 轮）

| 项 | 提交 | 说明 |
|----|------|------|
| 报告 | bb271d4 | docs(courtier): add the round-1 multi-dimension review report |
| P1-1 | c3c8964 | fix(setup): COURTIER_SETUP_KEY 权威化（内网来源不再绕过） |
| P2-1 | 7983636 | fix(auth): naive locked_until 归一化（锁定账号 500→403）+ 回归测试 |
| P1-2 | 678668b | fix(sse): 逐事件隔离 + render_sse_line default=str 兜底 + 测试 |
| P2-2 | 46f54f4 | fix(media): 转码结果 bucket 白名单（含负向测试） |
| P2-4 | eae8184 | fix(upload): 上传分块有界读取（聊天+资源库两通道） |
| P2-3 | 92d5a07 | fix(es): bulk item 级错误与 delete failures 上抛；删除先 ES 后 DB（fail-closed）；ES 异常不再透传客户端（P3-9） |
| P2-6/P2-7/P2-8 + 新发现 | e710e4d | fix(plugin): 连接超时；插件输出落 transfer bucket（原落 docs 桶且 presign 不放行——端到端断裂+永久孤儿，R1 新发现）；stop 竞态；续轮 IndexError；transfer e2e 无凭据时 skip |
| P2-5 | c645903 | feat(security): 可信代理 XFF 限速分桶（右条目防伪造）+ 测试 |
| P3-1/P3-2 | 399f27b | fix(plugin): 插件面错误不携带 exc 详情；on_progress 回调异常记日志 |
| P2-12/P3-3/P3-18 | 134bcd4 | refactor(db): 解循环导入；删 common/benchmarks；回归套件去 integration 标 |
| P3-4 | (docs) | AGENTS.md 目录树对齐实际、移除 CLAUDE.md 引用 |
| P3-5 | 398c873 | style: ruff 清零（94→0） |
| P3-6 | (多处) | mypy 60→55（settings_store 客户端复用、no-redef、Literal cast、import-as 重绑） |
| P1-3/P1-4/P2-10/P2-11/P3-10 | 074432e + d074c17 | DOMPurify 升级过 3.4.12 + npm audit 清零 + @types/dompurify 移除；minio/uv 镜像钉版本；search 插件 ES 客户端 <9 上限 |
| P3-7 | (security 提交) | ?token= 仅限 SSE/metrics 路径 + 单测 |
| P3-8 | 57eb494 | SDK resolve_file 拒绝非中转桶（深度防御） |
| P3-11 | 57eb494 | 资源库上传接 magic-byte 门 + 测试 |
| P2-9 | 788c990 + (memory) | /api/stop、改密、/api/memory 路由权限测试 |
| **R1 新发现 P1** | 3ab2870 | **fix(auth): refresh token 重用检测的家族撤销被 401 回滚吞掉——补测试时抓出的真漏洞** |

### 基线对比

- pytest：2302 → **2341 passed**（+39 个新测试，全部绿），6 skipped，0 failed
- ruff：94 → **0**
- mypy：60 → 55（剩余为类型严格度噪音，见 P3-6 记录）
- webui：npm audit 0 漏洞，npm test / build 通过
