global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]

  # The `courtier` app service is currently commented out in docker-compose.yml.
  # Uncomment the block below after enabling the `app` service.
  # - job_name: "courtier"
  #   static_configs:
  #     - targets: ["app:8000"]
