errors:
  # —— 工具执行 ——
  tool_timeout: "工具执行超时（{{ timeout_seconds }}秒）：{{ tool_name }}"
  tool_arg_parse: "工具 {{ tool_name }} 的参数解析失败。原始参数：{{ raw_arguments }}"
  tool_exception: "工具 {{ tool_name }} 执行异常：{{ error }}"
  tool_not_found: "工具 {{ tool_name }} 未注册，无法调用。当前可用工具：{{ available_tools }}。请改用可用工具，或直接给出文本回答。"
  tool_max_calls_exceeded: "工具 {{ tool_name }} 已被调用 {{ count }} 次，超过上限 {{ limit }} 次。请减少不必要的调用。"
  tool_max_consecutive_exceeded: "工具 {{ tool_name }} 已连续调用 {{ count }} 次，超过连续上限 {{ limit }} 次。请调整调用方式。"
  tool_missing_param: "工具 {{ tool_name }} 缺少必填参数 {{ param_name }}{% if hint %}（{{ hint }}）{% endif %}。"
  result_unknown_error: "{{ actor_name }} 执行失败，但未返回具体错误信息。"
  artifact_store_unavailable: "Artifact store 不可用，无法{{ action }}。"
  # —— 循环层 ——
  permission_denied: "权限不足，以下工具被拒绝调用：{{ denied_tools }}"
  internal_error: "内部错误：{{ message }}"
  model_error: "模型调用失败：{{ error }}"
  # —— 技能 / 子代理 ——
  skill_input_validation: "技能 {{ skill_name }} 的输入参数校验失败：\n{{ error_details }}\n请修正上述字段后重新调用。"
  skill_missing_mode: "技能 {{ skill_name }} 缺少 mode 参数。请指定 mode=\"subagent\"（复杂任务）或 mode=\"inline\"（简单任务）。"
  skill_runtime_unavailable: "技能 {{ skill_name }} 无法执行：AgentRuntime 不可用。"
  skill_spawn_failed: "技能 {{ skill_name }} 启动失败：{{ error }}"
  skill_failed: "技能 {{ skill_name }} 执行失败：{{ error }}"
  skill_inline_missing_tools: "inline 模式无法执行技能 {{ skill_name }}：缺少必要工具 {{ missing_tools }}。请使用 mode='subagent' 启动独立子代理执行，或通过 list_artifacts / get_artifact 从 artifact store 获取数据。"
  subagent_unknown: "未知的子代理或技能：{{ agent_name }}"
  subagent_timeout: "子代理执行超时（{{ timeout_seconds }}秒）"
  subagent_terminated: "子代理已被终止"
  subagent_budget_exhausted: "累计运行时间预算已耗尽（{{ used_seconds }}s / {{ limit_seconds }}s）"
  subagent_spawn_refused: "无法启动子代理 {{ agent_name }}：{{ reason }}"
  subagent_failed: "子代理执行失败：{{ error }}"
  # —— 插件通道 ——
  plugin_arg_path_escape: "参数 {{ param_name }} 的路径越出上传目录，已拒绝: {{ value }}"
  plugin_arg_file_missing: "参数 {{ param_name }} 指向的文件不存在: {{ value }}"
  plugin_file_transfer_failed: "文件转存对象存储失败: {{ error }}"
  plugin_bad_response_type: "插件 {{ plugin_name }} 的工具 {{ tool_name }} 返回了非预期的响应类型: {{ response_type }}"
  plugin_missing_success_field: "插件 {{ plugin_name }} 的工具 {{ tool_name }} 响应缺少 'success' 字段"
  plugin_call_timeout: "插件 '{{ plugin_name }}' 调用超时（{{ timeout }}s），请重试或拆分任务"
  plugin_crashed: "插件 '{{ plugin_name }}' 崩溃或已断开连接"
  # —— 内置工具 ——
  memory_unavailable: "记忆功能不可用（当前上下文管理器不支持记忆层）。"
  activate_domain_not_wired: "激活器未接线：activate_domain 需要在编排器构建时绑定 DomainActivator"
  # —— 工件绑定 / 投影 ——
  binder_field_unresolved: "无法解析必填字段 {{ field_name }}（类型 {{ artifact_type }}）"
  binder_text_below_min: "字段 {{ field_name }} 的文本长度 {{ length }} 低于最小长度 {{ min_chars }} 字符"
  binder_text_above_max: "字段 {{ field_name }} 的文本长度 {{ length }} 超过最大长度 {{ max_chars }} 字符"
  binder_items_below_min: "字段 {{ field_name }} 只有 {{ length }} 项，低于最小数量 {{ min_items }}"
  binder_items_above_max: "字段 {{ field_name }} 有 {{ length }} 项，超过最大数量 {{ max_items }}"
  binder_item_text_below_min: "字段 {{ field_name }} 第 {{ index }} 项长度 {{ length }} 字符，低于最小长度 {{ item_min_chars }} 字符"
  artifact_ref_not_found_hint: "该引用不存在或不属于当前任务可见范围，请使用当前任务链中工具实际返回的 result_id，或先调用 list_artifacts 查看可用工件。"
