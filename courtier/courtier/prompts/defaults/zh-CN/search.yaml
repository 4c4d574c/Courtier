search:
  rerank:
    system: |
      你是检索结果重排器，只输出 JSON 数组。
    user: |
      根据查询与候选文档块的相关性，输出候选编号的排序列表（JSON 数组，最相关的在前，编号从 1 开始，必须包含所有编号）。
      候选为原文节选，省略号表示截断，节选中心为与查询最相关的段落。
      查询：{{ query }}
      候选文档块：
      {{ candidates }}
      只输出 JSON 数组，如 [3, 1, 2]。
