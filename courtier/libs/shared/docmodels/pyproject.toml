[project]
name = "docmodels"
version = "1.0.0"
description = "Document and format-spec data models shared by core, libs, and plugins"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
