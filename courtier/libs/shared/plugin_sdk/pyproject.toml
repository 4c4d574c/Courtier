[project]
name = "courtier-plugin-sdk"
version = "0.1.0"
description = "SDK for building Courtier plugins — JSON-RPC runtime, host-service clients, and shared types"
requires-python = ">=3.12"
dependencies = [
    "minio>=7.2.0",
    "pydantic>=2.13.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
