[project]
name = "courtier-plugin-search"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["elasticsearch>=8.0.0", "courtier"]

[tool.uv.sources]
courtier = { path = "../../../../../..", editable = true }
